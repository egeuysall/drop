const Landing = () => {
  return (
    <main className="flex flex-col gap-18">
      {/* Hero Section */}
      <section className="gap-lg flex w-full flex-col items-center"></section>
    </main>
  );
};

export default Landing;
