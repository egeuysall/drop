// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: tracked_items.sql

package supabase

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createTrackedItem = `-- name: CreateTrackedItem :one
INSERT INTO tracked_items (
    user_id, 
    url, 
    name, 
    current_price, 
    target_price, 
    in_stock
) VALUES (
    $1, $2, $3, $4, $5, $6
) RETURNING id, user_id, url, name, current_price, target_price, in_stock, created_at, last_checked_at
`

type CreateTrackedItemParams struct {
	UserID       pgtype.UUID
	Url          string
	Name         string
	CurrentPrice pgtype.Numeric
	TargetPrice  pgtype.Numeric
	InStock      pgtype.Bool
}

// Create a new tracked item
func (q *Queries) CreateTrackedItem(ctx context.Context, arg CreateTrackedItemParams) (TrackedItem, error) {
	row := q.db.QueryRow(ctx, createTrackedItem,
		arg.UserID,
		arg.Url,
		arg.Name,
		arg.CurrentPrice,
		arg.TargetPrice,
		arg.InStock,
	)
	var i TrackedItem
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Url,
		&i.Name,
		&i.CurrentPrice,
		&i.TargetPrice,
		&i.InStock,
		&i.CreatedAt,
		&i.LastCheckedAt,
	)
	return i, err
}

const deleteTrackedItem = `-- name: DeleteTrackedItem :exec
DELETE FROM tracked_items 
WHERE id = $1 AND user_id = $2
`

type DeleteTrackedItemParams struct {
	ID     pgtype.UUID
	UserID pgtype.UUID
}

// Delete a tracked item
func (q *Queries) DeleteTrackedItem(ctx context.Context, arg DeleteTrackedItemParams) error {
	_, err := q.db.Exec(ctx, deleteTrackedItem, arg.ID, arg.UserID)
	return err
}

const getTrackedItem = `-- name: GetTrackedItem :one
SELECT id, user_id, url, name, current_price, target_price, in_stock, created_at, last_checked_at FROM tracked_items 
WHERE id = $1 AND user_id = $2
`

type GetTrackedItemParams struct {
	ID     pgtype.UUID
	UserID pgtype.UUID
}

// Get a tracked item by ID
func (q *Queries) GetTrackedItem(ctx context.Context, arg GetTrackedItemParams) (TrackedItem, error) {
	row := q.db.QueryRow(ctx, getTrackedItem, arg.ID, arg.UserID)
	var i TrackedItem
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Url,
		&i.Name,
		&i.CurrentPrice,
		&i.TargetPrice,
		&i.InStock,
		&i.CreatedAt,
		&i.LastCheckedAt,
	)
	return i, err
}

const getTrackedItemsDueForCheck = `-- name: GetTrackedItemsDueForCheck :many
SELECT id, user_id, url, name, current_price, target_price, in_stock, created_at, last_checked_at FROM tracked_items 
WHERE last_checked_at < NOW() - INTERVAL '6 hours'
ORDER BY last_checked_at ASC
`

// Get tracked items that need price checking (older than 6 hours)
func (q *Queries) GetTrackedItemsDueForCheck(ctx context.Context) ([]TrackedItem, error) {
	rows, err := q.db.Query(ctx, getTrackedItemsDueForCheck)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []TrackedItem
	for rows.Next() {
		var i TrackedItem
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Url,
			&i.Name,
			&i.CurrentPrice,
			&i.TargetPrice,
			&i.InStock,
			&i.CreatedAt,
			&i.LastCheckedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTrackedItems = `-- name: ListTrackedItems :many
SELECT id, user_id, url, name, current_price, target_price, in_stock, created_at, last_checked_at FROM tracked_items 
WHERE user_id = $1 
ORDER BY created_at DESC
`

// List all tracked items for a user
func (q *Queries) ListTrackedItems(ctx context.Context, userID pgtype.UUID) ([]TrackedItem, error) {
	rows, err := q.db.Query(ctx, listTrackedItems, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []TrackedItem
	for rows.Next() {
		var i TrackedItem
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Url,
			&i.Name,
			&i.CurrentPrice,
			&i.TargetPrice,
			&i.InStock,
			&i.CreatedAt,
			&i.LastCheckedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateTrackedItem = `-- name: UpdateTrackedItem :one
UPDATE tracked_items SET
    url = $2,
    name = $3,
    current_price = $4,
    target_price = $5,
    in_stock = $6,
    last_checked_at = NOW()
WHERE id = $1 AND user_id = $7
RETURNING id, user_id, url, name, current_price, target_price, in_stock, created_at, last_checked_at
`

type UpdateTrackedItemParams struct {
	ID           pgtype.UUID
	Url          string
	Name         string
	CurrentPrice pgtype.Numeric
	TargetPrice  pgtype.Numeric
	InStock      pgtype.Bool
	UserID       pgtype.UUID
}

// Update a tracked item
func (q *Queries) UpdateTrackedItem(ctx context.Context, arg UpdateTrackedItemParams) (TrackedItem, error) {
	row := q.db.QueryRow(ctx, updateTrackedItem,
		arg.ID,
		arg.Url,
		arg.Name,
		arg.CurrentPrice,
		arg.TargetPrice,
		arg.InStock,
		arg.UserID,
	)
	var i TrackedItem
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Url,
		&i.Name,
		&i.CurrentPrice,
		&i.TargetPrice,
		&i.InStock,
		&i.CreatedAt,
		&i.LastCheckedAt,
	)
	return i, err
}

const updateTrackedItemPrice = `-- name: UpdateTrackedItemPrice :one
UPDATE tracked_items SET
    current_price = $2,
    last_checked_at = NOW()
WHERE id = $1 AND user_id = $3
RETURNING id, user_id, url, name, current_price, target_price, in_stock, created_at, last_checked_at
`

type UpdateTrackedItemPriceParams struct {
	ID           pgtype.UUID
	CurrentPrice pgtype.Numeric
	UserID       pgtype.UUID
}

// Update only the price of a tracked item
func (q *Queries) UpdateTrackedItemPrice(ctx context.Context, arg UpdateTrackedItemPriceParams) (TrackedItem, error) {
	row := q.db.QueryRow(ctx, updateTrackedItemPrice, arg.ID, arg.CurrentPrice, arg.UserID)
	var i TrackedItem
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Url,
		&i.Name,
		&i.CurrentPrice,
		&i.TargetPrice,
		&i.InStock,
		&i.CreatedAt,
		&i.LastCheckedAt,
	)
	return i, err
}
